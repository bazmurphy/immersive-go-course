0 * * * * * echo "here is a command running every 0th second"
5 * * * * * echo "here is a command running every 5th second"
10 * * * * * echo "here is a command running every 10th second"
15 * * * * * echo "here is a command running every 15th second"
20 * * * * * echo "here is a command running every 20th second"
25 * * * * * echo "here is a command running every 25th second"
30 * * * * * echo "here is a command running every 30th second"
35 * * * * * echo "here is a command running every 35th second"
40 * * * * * echo "here is a command running every 40th second"
45 * * * * * echo "here is a command running every 45th second"
50 * * * * * echo "here is a command running every 50th second"
55 * * * * * echo "here is a command running every 55th second"

@REM 0 0 * * * * echo "every 0th second of the 0th minute of every hour"
@REM 0 5 * * * * echo "every 0th second of the 5th minute of every hour"
@REM 0 10 * * * * echo "every 0th second of the 10th minute of every hour"
@REM 0 15 * * * * echo "every 0th second of the 15th minute of every hour"
@REM 0 20 * * * * echo "every 0th second of the 20th minute of every hour"
@REM 0 25 * * * * echo "every 0th second of the 25th minute of every hour"
@REM 0 30 * * * * echo "every 0th second of the 30th minute of every hour"
@REM 0 35 * * * * echo "every 0th second of the 35th minute of every hour"
@REM 0 40 * * * * echo "every 0th second of the 40th minute of every hour"
@REM 0 45 * * * * echo "every 0th second of the 45th minute of every hour"
@REM 0 50 * * * * echo "every 0th second of the 50th minute of every hour"
@REM 0 55 * * * * echo "every 0th second of the 55th minute of every hour"